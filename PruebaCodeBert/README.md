Librerias necesarias para el modelo en CodeBERT

transformers: pip install transformers

pytorch con soporte CUDA: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

pandas: pip install pandas

numpy: pip install numpy
scikit-learn: pip install scikit-lea
rn

tqdm: pip install tqdm

datasets: pip install datasets

accelerate: pip install accelerate

tensorboard: pip install tensorboard

SE RECOMIENDA UTILIZAR UN ENTORNO VIRTUAL
python utilizado 3.9.13

CABECERAS DEL DATASET
id, text, question_code_type, tags, class